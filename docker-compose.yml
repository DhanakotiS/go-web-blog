services:
  mongo:
    image: mongo:latest
    ports:
      - 27017:27017

  app-server:
    image: app-server:latest
    ports:
      - 8080:8080
    depends_on:
      - mongo
    environment:
      MONGO_URI: mongodb://mongo:27017